#include <Arduino.h>

void setup() {
    // Start Serial
    Serial.begin(115200);                    // Initialize serial communication at 115200 baud rate
    Serial.println("Setup complete");        // Print a message indicating setup is complete
}

void loop() {
    // Do stuff...
}
